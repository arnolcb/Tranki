// src/config/production.js
export const PRODUCTION_CONFIG = {
  // Usar claves con restricciones específicas
  useRestrictedKeys: true,
  // Habilitar ofuscación
  obfuscateKeys: true,
  // Validar bundle signature
  validateSignature: true
};